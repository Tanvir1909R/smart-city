'use strict';

/**
 * energyenvironment service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::energyenvironment.energyenvironment');
